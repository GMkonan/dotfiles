function install_brew_apps {
    brew install node
} 

function install_brew_cask_apps {
    # other's apps that should be added:
    # vscode, virtualbox, zsh
    brew cask install google-chrome
    brew cask install spotify
}