### Konan dotfiles
- kitty (probably inside a .config)
- nix
    - 
- mac
    - brewfile
- devcontainer

### structure
- config => only has stuff that goes inside the .config folder (nvim, kitty)





